{
  "name": "lvovd",
  "version": "2.4.0",
  "private": true,
  "description": "A Locally Hosted Video Downloader Which You'll Use Responsibly",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app-server.js && node --check failure-classification.js && node --check history-store.js && node --check download-history.js && node --check ytdlp-manager.js && node --check scripts/launch.js && node --check scripts/update-ytdlp.js && node --check public/app.js && node --test test/*.test.js",
    "update-ytdlp": "node scripts/update-ytdlp.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "Apache-2.0"
}
